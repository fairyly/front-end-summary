#  函数封装写法

* 1
```
(function(){
  
  var datepicker = {};
  datepicker.getMonthData = function (year, month) {
    
    
    return {
      year: year,
      month: month,
      days: ret
    };
  }
  
  window.datepicker = datepicker;
})();
```
